[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short --cov=src --cov=app --cov-report=term-missing
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning
